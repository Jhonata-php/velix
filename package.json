{
  "name": "velix",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
