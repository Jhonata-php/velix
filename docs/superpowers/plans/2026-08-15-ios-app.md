# App iOS nativo — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development pra executar tarefa por tarefa. Verificação visual final (simulador) é feita pelo controlador da sessão, não por subagente — ele já tem acesso às ferramentas do simulador.

**Goal:** app iOS nativo (SwiftUI, iOS 17+) com as seis telas da spec de UX, rodando de verdade no simulador.

**Architecture:** SwiftUI + `@Observable`, `URLSession`/`async-await` pra rede, Keychain pra token, Firebase Cloud Messaging pra push. Ver `docs/superpowers/specs/2026-08-15-ios-app-design.md` pra decisões técnicas e `docs/superpowers/specs/2026-08-15-mobile-app-product-ux-design.md` pra fluxo/telas.

**Tech Stack:** Swift 6, SwiftUI, XCTest, Firebase SDK (Swift Package Manager).

## Nota sobre este plano

Diferente do plano do backend (que tinha código completo em cada passo), aqui as tarefas de UI (SwiftUI) especificam o contrato funcional exato — o que a tela precisa fazer, quais campos, quais chamadas de API, qual navegação — sem o código-fonte SwiftUI completo em cada view. Motivo: SwiftUI é verificável visualmente de forma muito mais direta e barata (rodar no simulador e olhar) do que lendo código, ao contrário da lógica pura do backend (onde o texto do plano era a única forma de garantir comportamento correto antes de existir um servidor de verdade pra testar). As camadas que **não** são UI (modelos, cliente de API, Keychain) continuam com código completo, porque são a base que todas as telas dependem e onde um erro sutil se propaga silenciosamente.

## Global Constraints

- Bundle ID: `com.velix.app`. Nome do app: "Velix".
- iOS mínimo: 17.0.
- Comentários no código, quando necessários, em português (mesmo padrão do resto do projeto).
- Base URL da API vem de `Instance.baseUrl` (por instância) — nunca hardcoded.
- Toda chamada autenticada usa `Authorization: Bearer <token>`.
- Contratos de API (nomes de campo, tipos) devem bater exatamente com os DTOs do backend NestJS (`apps/api/src/**/dto/*.ts` e os `select`/retornos dos services) — ver seção 2 deste plano pros contratos já mapeados.

---

### Task 1: Projeto Xcode + estrutura de pastas

**Files:**
- Create: `apps/ios/Velix.xcodeproj/project.pbxproj` (via `xcodebuild`/template, não escrito à mão)
- Create: `apps/ios/Velix/VelixApp.swift`
- Create: `apps/ios/Velix/Assets.xcassets/` (ícone provisório usando `apps/web/public/logo.png`/`logo-square.png` já existentes, redimensionados)
- Create: `apps/ios/VelixTests/` (target de teste vazio, pronto pra receber os testes das próximas tasks)

**Interfaces:**
- Produces: um projeto Xcode que builda vazio (`xcodebuild build` verde) antes de qualquer feature — todas as tasks seguintes assumem que este esqueleto já existe e builda.

- [ ] **Step 1: Criar o projeto**

Usar o template padrão do Xcode via linha de comando (`xcodebuild` não cria projetos novos sozinho — usar `xcrun` não serve aqui; a forma correta é o instalador criar a estrutura manualmente com um `project.pbxproj` mínimo, OU abrir o Xcode uma vez via automação. Dado que este ambiente não tem UI interativa do Xcode, a abordagem é: criar a estrutura de arquivos e um `project.pbxproj` gerado por `xcodegen` se disponível, ou escrito a mão seguindo o formato mínimo de um App SwiftUI de plataforma única. Se `xcodegen` não estiver instalado (`which xcodegen`), instalar via `brew install xcodegen` (ferramenta de terceiro aceitável aqui porque gerar `.pbxproj` à mão é impraticável e propenso a erro — não é uma dependência do *app*, é uma ferramenta de build usada uma vez pra gerar o projeto).

Com `xcodegen`, criar `apps/ios/project.yml`:

```yaml
name: Velix
options:
  bundleIdPrefix: com.velix
  deploymentTarget:
    iOS: "17.0"
targets:
  Velix:
    type: application
    platform: iOS
    sources: [Velix]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.velix.app
        MARKETING_VERSION: "1.0.0"
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
    info:
      path: Velix/Info.plist
      properties:
        UIBackgroundModes: [remote-notification]
    dependencies: []
  VelixTests:
    type: bundle.unit-test
    platform: iOS
    sources: [VelixTests]
    dependencies:
      - target: Velix
```

Rodar `cd apps/ios && xcodegen generate` — isso escreve `Velix.xcodeproj/`.

- [ ] **Step 2: `VelixApp.swift` mínimo**

```swift
import SwiftUI

@main
struct VelixApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Velix")
        }
    }
}
```

- [ ] **Step 3: Confirmar que builda**

Run: `cd apps/ios && xcodebuild -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16' build`
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 4: Commit**

```bash
git add apps/ios
git commit -m "App iOS: esqueleto do projeto (xcodegen, builda vazio)"
```

---

### Task 2: Modelos (DTOs) + cliente de API

**Files:**
- Create: `apps/ios/Velix/Core/Models.swift`
- Create: `apps/ios/Velix/Core/APIClient.swift`
- Create: `apps/ios/VelixTests/APIClientTests.swift`

**Interfaces:**
- Produces: `APIClient` com métodos genéricos `get<T: Decodable>(_ path: String) async throws -> T`, `post<T: Decodable, B: Encodable>(_ path: String, body: B) async throws -> T`, `put`/`delete` equivalentes; erro tipado `APIError` (mensagem do backend quando disponível). Todos os DTOs de `Models.swift` — usados por toda tela.

**Contratos exatos do backend** (não inventar campo, usar exatamente estes):

```
POST /auth/login
  body: { email: String, password: String, totpCode: String?, rememberMe: Bool? }
  200 sucesso: { accessToken: String, user: { name, email, role } }
  200 com reason (login incompleto, não é erro HTTP): { reason: "totp_required" | "totp_invalid" }

GET /servers  → [{ id, name, status, publicIp?, hostname?, dockerInstalled, metrics: { loadAvg, memUsedMb, memTotalMb, diskPercent, cpuPercent?, temperatureCelsius? }? }]

GET /servers/:id/metrics/history?hours=24 → [{ loadAvg1?, memUsedMb?, memTotalMb?, diskPercent?, cpuPercent?, temperatureCelsius?, capturedAt }]

GET /servers/:id/docker  (dockerStatus) → [{ id, names, image, status }]   // ver servers.service.ts:396 pro shape exato antes de codar — usar exatamente os nomes de campo de lá, não adivinhar

GET /alerts/thresholds → AlertThresholdPreference | null
PUT /alerts/thresholds  body: { cpuPercent?, memoryPercent?, temperatureCelsius?, dockerScope?, dockerEnabled? } → AlertThresholdPreference
GET /servers/:id/alerts/thresholds → AlertThresholdPreference | null  (já cai pro global se não houver override, ver Fix 3 da revisão do backend)
PUT /servers/:id/alerts/thresholds → AlertThresholdPreference

POST /push/devices  body: { platform: "ios", fcmToken: String } → { ok: true }
DELETE /push/devices/:id → { ok: true }
```

`AlertThresholdPreference`: `{ id, userId, serverId: String?, cpuPercent: Int?, memoryPercent: Int?, temperatureCelsius: Int?, dockerScope: "all" | "managed_apps", dockerEnabled: Bool }`.

- [ ] **Step 1: Escrever o teste primeiro**

`apps/ios/VelixTests/APIClientTests.swift`:

```swift
import XCTest
@testable import Velix

final class APIClientTests: XCTestCase {
    func testDecodesLoginSuccess() throws {
        let json = #"{"accessToken":"abc123","user":{"name":"Ana","email":"ana@x.com","role":"admin"}}"#.data(using: .utf8)!
        let decoded = try JSONDecoder().decode(LoginResponse.self, from: json)
        XCTAssertEqual(decoded.accessToken, "abc123")
        XCTAssertEqual(decoded.user?.email, "ana@x.com")
        XCTAssertNil(decoded.reason)
    }

    func testDecodesTotpRequired() throws {
        let json = #"{"reason":"totp_required"}"#.data(using: .utf8)!
        let decoded = try JSONDecoder().decode(LoginResponse.self, from: json)
        XCTAssertNil(decoded.accessToken)
        XCTAssertEqual(decoded.reason, "totp_required")
    }

    func testDecodesServerSummaryWithMissingMetrics() throws {
        let json = #"{"id":"s1","name":"srv1","status":"ONLINE","dockerInstalled":true}"#.data(using: .utf8)!
        let decoded = try JSONDecoder().decode(ServerSummary.self, from: json)
        XCTAssertEqual(decoded.id, "s1")
        XCTAssertNil(decoded.metrics)
    }

    func testBuildsAuthorizedRequest() {
        let client = APIClient(baseURL: URL(string: "https://painel.exemplo.com")!, token: "tok123")
        let request = client.makeRequest(path: "/servers", method: "GET")
        XCTAssertEqual(request.url?.absoluteString, "https://painel.exemplo.com/servers")
        XCTAssertEqual(request.value(forHTTPHeaderField: "Authorization"), "Bearer tok123")
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha** (módulo `Velix` ainda não expõe esses tipos)

Run: `cd apps/ios && xcodebuild test -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: falha de compilação (`LoginResponse`/`ServerSummary`/`APIClient` não existem)

- [ ] **Step 3: Implementar `Models.swift`**

```swift
import Foundation

struct LoginResponse: Decodable {
    let accessToken: String?
    let user: LoggedUser?
    let reason: String?
}

struct LoggedUser: Decodable {
    let name: String
    let email: String
    let role: String
}

struct ServerMetrics: Decodable {
    let loadAvg1: Double?
    let memUsedMb: Int?
    let memTotalMb: Int?
    let diskPercent: Double?
    let cpuPercent: Double?
    let temperatureCelsius: Double?
}

struct ServerSummary: Decodable, Identifiable {
    let id: String
    let name: String
    let status: String
    let publicIp: String?
    let hostname: String?
    let dockerInstalled: Bool
    let metrics: ServerMetrics?
}

struct MetricSample: Decodable, Identifiable {
    var id: String { capturedAt }
    let loadAvg1: Double?
    let memUsedMb: Int?
    let memTotalMb: Int?
    let diskPercent: Double?
    let cpuPercent: Double?
    let temperatureCelsius: Double?
    let capturedAt: String
}

struct ContainerStatus: Decodable, Identifiable {
    var id: String { names }
    let names: String
    let image: String
    let status: String
}

struct AlertThresholdPreference: Codable {
    var id: String?
    var userId: String?
    var serverId: String?
    var cpuPercent: Int?
    var memoryPercent: Int?
    var temperatureCelsius: Int?
    var dockerScope: String
    var dockerEnabled: Bool
}

struct APIErrorBody: Decodable {
    let message: String?
}
```

(Se `dockerStatus` do backend tiver um shape diferente do assumido em `ContainerStatus`, leia `apps/api/src/servers/servers.service.ts` — método `dockerStatus`, por volta da linha 396 — antes de fixar os nomes de campo, e ajuste aqui pra bater exatamente.)

- [ ] **Step 4: Implementar `APIClient.swift`**

```swift
import Foundation

enum APIError: Error, LocalizedError {
    case http(status: Int, message: String?)
    case decoding(Error)
    case network(Error)

    var errorDescription: String? {
        switch self {
        case .http(_, let message): return message ?? "Erro ao falar com o servidor"
        case .decoding: return "Resposta inesperada do servidor"
        case .network: return "Sem conexão com o servidor"
        }
    }
}

final class APIClient {
    let baseURL: URL
    var token: String?

    init(baseURL: URL, token: String? = nil) {
        self.baseURL = baseURL
        self.token = token
    }

    func makeRequest(path: String, method: String) -> URLRequest {
        var request = URLRequest(url: baseURL.appendingPathComponent(path))
        request.httpMethod = method
        if let token {
            request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        return request
    }

    private func send<T: Decodable>(_ request: URLRequest) async throws -> T {
        let data: Data
        let response: URLResponse
        do {
            (data, response) = try await URLSession.shared.data(for: request)
        } catch {
            throw APIError.network(error)
        }
        guard let http = response as? HTTPURLResponse else {
            throw APIError.network(URLError(.badServerResponse))
        }
        guard (200..<300).contains(http.statusCode) else {
            let body = try? JSONDecoder().decode(APIErrorBody.self, from: data)
            throw APIError.http(status: http.statusCode, message: body?.message)
        }
        do {
            return try JSONDecoder().decode(T.self, from: data)
        } catch {
            throw APIError.decoding(error)
        }
    }

    func get<T: Decodable>(_ path: String) async throws -> T {
        try await send(makeRequest(path: path, method: "GET"))
    }

    func post<T: Decodable, B: Encodable>(_ path: String, body: B) async throws -> T {
        var request = makeRequest(path: path, method: "POST")
        request.httpBody = try JSONEncoder().encode(body)
        return try await send(request)
    }

    func put<T: Decodable, B: Encodable>(_ path: String, body: B) async throws -> T {
        var request = makeRequest(path: path, method: "PUT")
        request.httpBody = try JSONEncoder().encode(body)
        return try await send(request)
    }

    func delete(_ path: String) async throws {
        let _: EmptyResponse = try await send(makeRequest(path: path, method: "DELETE"))
    }
}

struct EmptyResponse: Decodable {}
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `cd apps/ios && xcodebuild test -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: `** TEST SUCCEEDED **`, os 4 testes passando.

- [ ] **Step 6: Commit**

```bash
git add apps/ios/Velix/Core/Models.swift apps/ios/Velix/Core/APIClient.swift apps/ios/VelixTests/APIClientTests.swift
git commit -m "App iOS: modelos e cliente de API"
```

---

### Task 3: Keychain + armazenamento de instâncias

**Files:**
- Create: `apps/ios/Velix/Core/KeychainStore.swift`
- Create: `apps/ios/Velix/Core/Instance.swift`
- Create: `apps/ios/VelixTests/KeychainStoreTests.swift`
- Create: `apps/ios/VelixTests/InstanceStoreTests.swift`

**Interfaces:**
- Consumes: nada de tasks anteriores.
- Produces: `KeychainStore.save(_:forKey:)`/`.read(forKey:)`/`.delete(forKey:)` (genérico, `Data` in/out); `Instance` struct (`id: UUID`, `baseURL: URL`, `displayName: String`, `userEmail: String`, `accessToken: String`); `InstanceStore` (`@Observable`) com `instances: [Instance]`, `activeInstance: Instance?`, `add(_:)`, `remove(_:)`, `setActive(_:)`, persistido via `KeychainStore` (serializa a lista como JSON numa chave única). Consumido por `AppSession` (Task 4) e todas as telas.

- [ ] **Step 1: Escrever os testes primeiro**

`apps/ios/VelixTests/KeychainStoreTests.swift`:

```swift
import XCTest
@testable import Velix

final class KeychainStoreTests: XCTestCase {
    let store = KeychainStore(service: "com.velix.app.tests")

    override func tearDown() {
        store.delete(forKey: "test-key")
        super.tearDown()
    }

    func testSaveAndRead() throws {
        let data = "segredo".data(using: .utf8)!
        store.save(data, forKey: "test-key")
        XCTAssertEqual(store.read(forKey: "test-key"), data)
    }

    func testReadMissingKeyReturnsNil() {
        XCTAssertNil(store.read(forKey: "nunca-existiu"))
    }

    func testDeleteRemoves() throws {
        store.save("x".data(using: .utf8)!, forKey: "test-key")
        store.delete(forKey: "test-key")
        XCTAssertNil(store.read(forKey: "test-key"))
    }

    func testOverwriteReplacesValue() throws {
        store.save("primeiro".data(using: .utf8)!, forKey: "test-key")
        store.save("segundo".data(using: .utf8)!, forKey: "test-key")
        XCTAssertEqual(store.read(forKey: "test-key"), "segundo".data(using: .utf8)!)
    }
}
```

`apps/ios/VelixTests/InstanceStoreTests.swift`:

```swift
import XCTest
@testable import Velix

final class InstanceStoreTests: XCTestCase {
    func testAddSetsAsActiveWhenFirst() {
        let store = InstanceStore(keychain: KeychainStore(service: "com.velix.app.tests.instances"))
        store.clearAllForTesting()
        let instance = Instance(id: UUID(), baseURL: URL(string: "https://a.com")!, displayName: "a.com", userEmail: "a@a.com", accessToken: "tok")
        store.add(instance)
        XCTAssertEqual(store.activeInstance?.id, instance.id)
        XCTAssertEqual(store.instances.count, 1)
    }

    func testRemoveActiveFallsBackToAnother() {
        let store = InstanceStore(keychain: KeychainStore(service: "com.velix.app.tests.instances"))
        store.clearAllForTesting()
        let i1 = Instance(id: UUID(), baseURL: URL(string: "https://a.com")!, displayName: "a.com", userEmail: "a@a.com", accessToken: "t1")
        let i2 = Instance(id: UUID(), baseURL: URL(string: "https://b.com")!, displayName: "b.com", userEmail: "b@b.com", accessToken: "t2")
        store.add(i1)
        store.add(i2)
        store.setActive(i1)
        store.remove(i1)
        XCTAssertEqual(store.activeInstance?.id, i2.id)
    }

    func testRemoveLastLeavesNoActive() {
        let store = InstanceStore(keychain: KeychainStore(service: "com.velix.app.tests.instances"))
        store.clearAllForTesting()
        let i1 = Instance(id: UUID(), baseURL: URL(string: "https://a.com")!, displayName: "a.com", userEmail: "a@a.com", accessToken: "t1")
        store.add(i1)
        store.remove(i1)
        XCTAssertNil(store.activeInstance)
        XCTAssertTrue(store.instances.isEmpty)
    }
}
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `cd apps/ios && xcodebuild test -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: falha de compilação (tipos não existem)

- [ ] **Step 3: Implementar `KeychainStore.swift`**

```swift
import Foundation
import Security

/// Wrapper fino sobre o Keychain do sistema — sem biblioteca de terceiro,
/// é a própria API que a Apple recomenda pra dados sensíveis.
final class KeychainStore {
    private let service: String

    init(service: String) {
        self.service = service
    }

    func save(_ data: Data, forKey key: String) {
        delete(forKey: key)
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: key,
            kSecValueData as String: data,
        ]
        SecItemAdd(query as CFDictionary, nil)
    }

    func read(forKey key: String) -> Data? {
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: key,
            kSecReturnData as String: true,
            kSecMatchLimit as String: kSecMatchLimitOne,
        ]
        var result: AnyObject?
        let status = SecItemCopyMatching(query as CFDictionary, &result)
        guard status == errSecSuccess else { return nil }
        return result as? Data
    }

    func delete(forKey key: String) {
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: key,
        ]
        SecItemDelete(query as CFDictionary)
    }
}
```

- [ ] **Step 4: Implementar `Instance.swift`**

```swift
import Foundation
import Observation

struct Instance: Codable, Identifiable, Equatable {
    let id: UUID
    let baseURL: URL
    let displayName: String
    let userEmail: String
    var accessToken: String
}

/// Lista de instâncias Velix logadas (ver spec de UX, seção 2 — "multi-servidor"
/// no app é multi-instância, não multi-servidor dentro de uma instância só).
@Observable
final class InstanceStore {
    private let keychain: KeychainStore
    private let storageKey = "instances"
    private let activeKey = "active-instance-id"

    private(set) var instances: [Instance] = []
    private(set) var activeInstance: Instance?

    init(keychain: KeychainStore) {
        self.keychain = keychain
        load()
    }

    func add(_ instance: Instance) {
        instances.append(instance)
        if activeInstance == nil {
            activeInstance = instance
        }
        persist()
    }

    func remove(_ instance: Instance) {
        instances.removeAll { $0.id == instance.id }
        if activeInstance?.id == instance.id {
            activeInstance = instances.first
        }
        persist()
    }

    func setActive(_ instance: Instance) {
        guard instances.contains(where: { $0.id == instance.id }) else { return }
        activeInstance = instance
        persist()
    }

    func clearAllForTesting() {
        instances = []
        activeInstance = nil
        persist()
    }

    private func persist() {
        if let data = try? JSONEncoder().encode(instances) {
            keychain.save(data, forKey: storageKey)
        }
        if let activeInstance, let idData = activeInstance.id.uuidString.data(using: .utf8) {
            keychain.save(idData, forKey: activeKey)
        } else {
            keychain.delete(forKey: activeKey)
        }
    }

    private func load() {
        guard let data = keychain.read(forKey: storageKey),
              let decoded = try? JSONDecoder().decode([Instance].self, from: data) else { return }
        instances = decoded
        if let idData = keychain.read(forKey: activeKey),
           let idString = String(data: idData, encoding: .utf8),
           let id = UUID(uuidString: idString) {
            activeInstance = instances.first { $0.id == id }
        } else {
            activeInstance = instances.first
        }
    }
}
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `cd apps/ios && xcodebuild test -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 6: Commit**

```bash
git add apps/ios/Velix/Core/KeychainStore.swift apps/ios/Velix/Core/Instance.swift apps/ios/VelixTests/KeychainStoreTests.swift apps/ios/VelixTests/InstanceStoreTests.swift
git commit -m "App iOS: Keychain e armazenamento de instâncias"
```

---

### Task 4: `AppSession` — estado global e decisão de tela raiz

**Files:**
- Create: `apps/ios/Velix/Core/AppSession.swift`
- Modify: `apps/ios/Velix/VelixApp.swift`

**Interfaces:**
- Consumes: `InstanceStore`, `KeychainStore`, `APIClient` (Tasks 2-3).
- Produces: `AppSession` (`@Observable`): `instanceStore: InstanceStore`, `apiClient(for instance: Instance) -> APIClient` (monta um `APIClient` configurado pra aquela instância), propriedade computada `hasAnyInstance: Bool`. `VelixApp` decide entre a pilha de onboarding e a `TabView` principal com base nisso — consumido por todas as telas via `@Environment`.

- [ ] **Step 1: Implementar `AppSession.swift`**

```swift
import Foundation
import Observation

@Observable
final class AppSession {
    let instanceStore: InstanceStore

    init(instanceStore: InstanceStore = InstanceStore(keychain: KeychainStore(service: "com.velix.app"))) {
        self.instanceStore = instanceStore
    }

    var hasAnyInstance: Bool {
        !instanceStore.instances.isEmpty
    }

    func apiClient(for instance: Instance) -> APIClient {
        APIClient(baseURL: instance.baseURL, token: instance.accessToken)
    }

    var activeAPIClient: APIClient? {
        guard let active = instanceStore.activeInstance else { return nil }
        return apiClient(for: active)
    }
}
```

- [ ] **Step 2: Atualizar `VelixApp.swift`**

```swift
import SwiftUI

@main
struct VelixApp: App {
    @State private var session = AppSession()

    var body: some Scene {
        WindowGroup {
            Group {
                if session.hasAnyInstance {
                    MainTabView()
                } else {
                    AddInstanceView()
                }
            }
            .environment(session)
        }
    }
}
```

(`MainTabView` e `AddInstanceView` ainda não existem — são das próximas tasks. Este arquivo só compila de verdade depois da Task 5 em diante; é esperado que `xcodebuild build` falhe até lá. Não é um problema desta task, é a ordem natural de construção incremental.)

- [ ] **Step 3: Commit**

```bash
git add apps/ios/Velix/Core/AppSession.swift apps/ios/Velix/VelixApp.swift
git commit -m "App iOS: AppSession e decisão de tela raiz"
```

---

### Task 5: Onboarding — adicionar instância, login, 2FA

**Files:**
- Create: `apps/ios/Velix/Features/Onboarding/AddInstanceView.swift`
- Create: `apps/ios/Velix/Features/Onboarding/LoginView.swift`
- Create: `apps/ios/Velix/Features/Onboarding/TwoFactorView.swift`

**Interfaces:**
- Consumes: `AppSession`/`InstanceStore` (Task 4), `APIClient`/`LoginResponse` (Task 2).
- Produces: fluxo completo de onboarding, terminando com `session.instanceStore.add(...)` de uma `Instance` válida — depois disso `VelixApp` naturalmente troca pra `MainTabView` (Task 6+) porque `hasAnyInstance` vira `true`.

**Requisitos funcionais** (ver `docs/superpowers/specs/2026-08-15-mobile-app-product-ux-design.md` seção 3 pro fluxo completo):

- `AddInstanceView`: campo de texto pro domínio (aceita com ou sem `https://` — normalizar adicionando `https://` se ausente antes de montar a `URL`), botão "Continuar" desabilitado enquanto o campo estiver vazio. Ao continuar, tenta um `GET` simples na base URL (ex.: `/health` se existir, senão qualquer rota leve — usar `GET /` e tratar qualquer resposta HTTP, mesmo 404, como "alcançável"; só erro de rede/DNS conta como "não alcançável") antes de navegar pra `LoginView`. Erro de alcance mostra mensagem inline, não navega.
- `LoginView`: campos e-mail/senha, toggle "Lembrar de mim", botão "Entrar". Chama `POST /auth/login` via um `APIClient` temporário (sem token) apontando pra base URL vinda da tela anterior. Se a resposta tiver `accessToken`, monta a `Instance` e adiciona ao `InstanceStore` (fluxo completo). Se `reason == "totp_required"` ou `"totp_invalid"`, navega pra `TwoFactorView` levando e-mail/senha/rememberMe adiante (o backend reprocessa o login completo com o código). Erro de credenciais mostra mensagem inline.
- `TwoFactorView`: campo de 6 dígitos (teclado numérico, `.keyboardType(.numberPad)`), botão "Confirmar", link "Usar código de recuperação" que troca o campo pra aceitar o formato do código de recuperação (texto livre, sem limite de 6 dígitos). Reenvia `POST /auth/login` com `totpCode` preenchido. Sucesso → mesma finalização do `LoginView` (monta `Instance`, adiciona ao store).

- [ ] **Step 1: Implementar as três views conforme os requisitos acima**

(Sem código-fonte completo prescrito aqui — ver a nota no topo do plano. Usar `@State` local pros campos de formulário, `@Environment(AppSession.self)` pra acessar a sessão, `NavigationStack` com `NavigationPath` ou `.navigationDestination` pra empilhar Login → TwoFactor.)

- [ ] **Step 2: Build**

Run: `cd apps/ios && xcodebuild build -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: `** BUILD SUCCEEDED **` (ainda vai falhar até `MainTabView` existir — Task 4 já avisou disso; se este for o caso, criar um `MainTabView` provisório com só `Text("TODO")` pra destravar o build até a Task 6 substituir de verdade, e diga isso claramente no commit/relatório).

- [ ] **Step 3: Commit**

```bash
git add apps/ios/Velix/Features/Onboarding
git commit -m "App iOS: fluxo de onboarding (adicionar instância, login, 2FA)"
```

---

### Task 6: Dashboard

**Files:**
- Create: `apps/ios/Velix/Features/Dashboard/DashboardView.swift`
- Create: `apps/ios/Velix/Features/Dashboard/ServerRow.swift`
- Create: `apps/ios/Velix/MainTabView.swift`

**Interfaces:**
- Consumes: `AppSession.activeAPIClient`, `ServerSummary` (Task 2).
- Produces: `MainTabView` (substitui o provisório da Task 5, se houver) com `TabView` de 3 abas: Dashboard, Notificações (placeholder até Task 8), Conta (placeholder até Task 9).

**Requisitos funcionais:**
- `DashboardView`: `List` de `ServerRow`, carregada via `GET /servers` no `.task`/`onAppear`, com pull-to-refresh (`.refreshable`). Estado de carregando/erro/vazio tratados.
- `ServerRow`: nome do servidor, bolinha de status (verde=ONLINE, cinza=OFFLINE/PENDING, vermelho=ERROR), CPU%/memória%/temperatura em texto pequeno quando presentes em `metrics` (omitir o que for `nil`, não mostrar "N/A"). Tap navega pra `ServerDetailView` (Task 7).

- [ ] **Step 1: Implementar `MainTabView`, `DashboardView`, `ServerRow`**

- [ ] **Step 2: Build**

Run: `cd apps/ios && xcodebuild build -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 3: Commit**

```bash
git add apps/ios/Velix/Features/Dashboard apps/ios/Velix/MainTabView.swift
git commit -m "App iOS: dashboard de servidores"
```

---

### Task 7: Detalhe do servidor (métricas + gráfico + containers)

**Files:**
- Create: `apps/ios/Velix/Features/ServerDetail/ServerDetailView.swift`

**Interfaces:**
- Consumes: `MetricSample`, `ContainerStatus` (Task 2), navegação vinda de `ServerRow` (Task 6).

**Requisitos funcionais:**
- Carrega `GET /servers/:id/metrics/history?hours=24` e `GET /servers/:id/docker` em paralelo (`async let`).
- Gráfico `Charts` (`Chart` + `LineMark`) de CPU% ao longo do tempo (uma linha; se quiser, memória numa segunda linha com cor diferente — não obrigatório nesta fase, CPU sozinho já cobre o requisito mínimo).
- Lista de containers abaixo do gráfico: nome, imagem, status.
- Botão/link "Abrir no navegador" que monta a URL do painel web pra aquele servidor (`{baseURL}/servers/{id}`) e abre via `UIApplication.shared.open(_:)` — não WebView embutida (ver spec de UX seção 4.2).

- [ ] **Step 1: Implementar**
- [ ] **Step 2: Build** — mesmo comando das tasks anteriores.
- [ ] **Step 3: Commit**

```bash
git add apps/ios/Velix/Features/ServerDetail
git commit -m "App iOS: detalhe do servidor com histórico e containers"
```

---

### Task 8: Configuração de notificações

**Files:**
- Create: `apps/ios/Velix/Features/Notifications/NotificationSettingsView.swift`
- Create: `apps/ios/Velix/Features/Notifications/ThresholdEditorView.swift`

**Interfaces:**
- Consumes: `AlertThresholdPreference` (Task 2), `GET`/`PUT /alerts/thresholds` e `/servers/:id/alerts/thresholds`.

**Requisitos funcionais** (ver spec de UX seção 4.3):
- `NotificationSettingsView`: bloco "Padrão" no topo (carrega/edita via `ThresholdEditorView` com `serverId: nil`), lista de servidores abaixo, cada um abrindo `ThresholdEditorView` com `serverId` daquele servidor (override) e um botão "Usar o padrão" que reseta o override (chamar `PUT` com todos os campos `nil`/desabilitado — ou, se o backend não suportar "remover override" via PUT com nulls, documentar essa limitação no relatório da task ao invés de inventar um comportamento).
- `ThresholdEditorView`: componente reusado (contexto global vs. override só muda o endpoint chamado, não a UI) — três campos numéricos com toggle de habilitado (CPU%, memória%, temperatura°C), toggle "Alertar sobre containers" + seletor "Todos" / "Só minhas aplicações", botão "Salvar". Pede permissão de notificação (`UNUserNotificationCenter.current().requestAuthorization`) na primeira vez que esta tela abre (não em `AddInstanceView`/onboarding).

- [ ] **Step 1: Implementar**
- [ ] **Step 2: Build**
- [ ] **Step 3: Commit**

```bash
git add apps/ios/Velix/Features/Notifications
git commit -m "App iOS: configuração de limites de alerta e permissão de notificação"
```

---

### Task 9: Instâncias (trocar/adicionar/remover)

**Files:**
- Create: `apps/ios/Velix/Features/Instances/InstanceListView.swift`

**Interfaces:**
- Consumes: `InstanceStore` (Task 3).

**Requisitos funcionais** (ver spec de UX seção 4.4):
- Lista de instâncias com a ativa marcada, tap troca a ativa (`instanceStore.setActive`).
- Botão "Adicionar instância" reabre o fluxo de `AddInstanceView` (Task 5) como uma sheet/navegação modal, não substituindo a tela toda.
- Swipe-to-delete ou botão remover por instância, chamando `instanceStore.remove(_:)` e, antes disso, `DELETE /push/devices/:id` daquela instância (usa o `APIClient` daquela instância especificamente, não da ativa) — se o ID do device token local não estiver salvo em lugar nenhum ainda, essa chamada de `DELETE` fica como próximo passo documentado no relatório da task (não é um requisito bloqueante desta tela, é do PushManager na Task 10).

- [ ] **Step 1: Implementar**
- [ ] **Step 2: Build**
- [ ] **Step 3: Commit**

```bash
git add apps/ios/Velix/Features/Instances
git commit -m "App iOS: tela de instâncias (trocar/adicionar/remover)"
```

---

### Task 10: Push notification (Firebase + APNs) e deep link

**Files:**
- Modify: `apps/ios/project.yml` (adicionar dependência SPM do Firebase)
- Create: `apps/ios/Velix/Core/PushManager.swift`
- Create: `apps/ios/Velix/AppDelegate.swift`
- Modify: `apps/ios/Velix/VelixApp.swift`

**Interfaces:**
- Consumes: `AppSession`, `APIClient.post` (Task 2), `Instance` (Task 3).
- Produces: registro de push funcional; `PushManager.handleNotificationTap(userInfo:)` decide navegação (deep link pro servidor).

- [ ] **Step 1: Adicionar a dependência do Firebase**

Em `apps/ios/project.yml`, adicionar sob o target `Velix`:

```yaml
    dependencies:
      - package: FirebaseMessaging
packages:
  FirebaseMessaging:
    url: https://github.com/firebase/firebase-ios-sdk
    from: 11.0.0
```

Rodar `cd apps/ios && xcodegen generate` de novo pra regerar o projeto com a dependência.

- [ ] **Step 2: `AppDelegate.swift`**

```swift
import UIKit
import FirebaseCore
import FirebaseMessaging
import UserNotifications

final class AppDelegate: NSObject, UIApplicationDelegate, UNUserNotificationCenterDelegate {
    func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
        FirebaseApp.configure()
        UNUserNotificationCenter.current().delegate = self
        application.registerForRemoteNotifications()
        return true
    }

    func application(_ application: UIApplication, didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
        Messaging.messaging().apnsToken = deviceToken
    }

    func userNotificationCenter(_ center: UNUserNotificationCenter, didReceive response: UNNotificationResponse, withCompletionHandler completionHandler: @escaping () -> Void) {
        PushManager.shared.handleNotificationTap(userInfo: response.notification.request.content.userInfo)
        completionHandler()
    }

    func userNotificationCenter(_ center: UNUserNotificationCenter, willPresent notification: UNNotification, withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void) {
        completionHandler([.banner, .sound])
    }
}
```

- [ ] **Step 3: `PushManager.swift`**

```swift
import Foundation
import FirebaseMessaging

/// Registra o token FCM em cada instância logada (ver spec de UX seção 7 —
/// mesmo token físico do aparelho, uma chamada de POST /push/devices por
/// instância, porque cada backend tem sua própria tabela de DeviceToken).
final class PushManager {
    static let shared = PushManager()

    struct RegisterDeviceBody: Encodable {
        let platform = "ios"
        let fcmToken: String
    }

    var pendingDeepLinkServerId: String?
    var pendingDeepLinkInstanceId: UUID?

    func registerCurrentToken(for instance: Instance, apiClient: APIClient) async {
        guard let token = Messaging.messaging().fcmToken else { return }
        _ = try? await apiClient.post("/push/devices", body: RegisterDeviceBody(fcmToken: token)) as EmptyResponse
    }

    func handleNotificationTap(userInfo: [AnyHashable: Any]) {
        if let serverId = userInfo["serverId"] as? String {
            pendingDeepLinkServerId = serverId
        }
    }
}
```

(Nota: o payload atual do backend não carrega identificador de instância — ver seção 7 da spec de UX. `pendingDeepLinkInstanceId` fica sem uso real até essa lacuna ser resolvida; documentar isso no relatório da task, não inventar um campo que o backend não manda.)

- [ ] **Step 4: Ligar ao `VelixApp`**

```swift
@main
struct VelixApp: App {
    @UIApplicationDelegateAdaptor(AppDelegate.self) var appDelegate
    @State private var session = AppSession()

    var body: some Scene {
        WindowGroup {
            Group {
                if session.hasAnyInstance {
                    MainTabView()
                } else {
                    AddInstanceView()
                }
            }
            .environment(session)
            .task {
                if let active = session.instanceStore.activeInstance {
                    await PushManager.shared.registerCurrentToken(for: active, apiClient: session.apiClient(for: active))
                }
            }
        }
    }
}
```

- [ ] **Step 5: Build**

Run: `cd apps/ios && xcodebuild build -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: `** BUILD SUCCEEDED **` (o simulador não recebe push de verdade — APNs real exige dispositivo físico ou provisionamento; a verificação aqui é só de compilação/wiring, não de entrega de notificação de fato).

- [ ] **Step 6: Commit**

```bash
git add apps/ios/project.yml apps/ios/Velix/Core/PushManager.swift apps/ios/Velix/AppDelegate.swift apps/ios/Velix/VelixApp.swift
git commit -m "App iOS: registro de push (Firebase) e deep link de notificação"
```

---

### Task 11: Verificação final — build, testes, simulador

**Files:** nenhum arquivo novo.

- [ ] **Step 1: Build completo**

Run: `cd apps/ios && xcodebuild build -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 2: Todos os testes**

Run: `cd apps/ios && xcodebuild test -project Velix.xcodeproj -scheme Velix -destination 'platform=iOS Simulator,name=iPhone 16'`
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 3: Verificação visual no simulador** (feita pelo controlador da sessão, que já tem acesso às ferramentas de simulador — não delega esta etapa a um subagente)

Instalar e abrir o app no simulador, navegar: tela de adicionar instância → (sem backend real disponível neste ambiente, documentar como limitação) → capturar screenshot de cada tela alcançável sem precisar de um login bem-sucedido de verdade (telas de formulário, pelo menos). Reportar quais telas foram confirmadas visualmente e quais dependem de um backend real rodando (fora do alcance desta verificação).

- [ ] **Step 4: Nenhum commit nesta task — é só verificação.**
